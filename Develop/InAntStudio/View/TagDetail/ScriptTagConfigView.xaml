<UserControl x:Class="InAntStudio.ScriptTagConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InAntStudio"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="32" />
        </Grid.ColumnDefinitions>
        <TextBox Text="{Binding Express,Mode=TwoWay,UpdateSourceTrigger=LostFocus}" CaretBrush="White" Margin="2" Height="60" HorizontalAlignment="Stretch" Background="#0FFFFFFF" Foreground="White"  AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        <StackPanel Grid.Column="1" Margin="2">
            <Button Content="..." VerticalContentAlignment="Top" Command="{Binding ExpressEditCommand}" />
            <Button Content="X" Command="{Binding ExpressClearCommand}"  Margin="0,2"/>
        </StackPanel>
    </Grid>
</UserControl>
